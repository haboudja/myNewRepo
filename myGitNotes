GIT COMMANDS

git init
git add .
git commit -m " message "

//create a working copy of a local repository
git clone /path/to/repository
or
git clone username@host:/path/to/repository

PUSHING CHANGES

git push origin master  (master can be anything you want it to be)



if you have not cloned an existing repository and want to connect your repositor to a remote server, you need to add it with

git remote add origin <server>


git pull
git merge <branch>
